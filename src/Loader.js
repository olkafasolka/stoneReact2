import React, { Component } from "react";
import "./Loader.css";

const Loader = () => (
  <div className="loadDiv">
    <img className="loader" src="/giphy.gif" />
  </div>
);
export default Loader;
